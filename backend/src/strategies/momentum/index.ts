/**
 * strategies/momentum/index.ts
 *
 * Momentum strategy placeholder.
 * This module is intentionally minimal — the momentum strategy will be
 * implemented in a future phase. The placeholder ensures the module path
 * exists and exports the expected interface shape for scalability.
 *
 * Planned approach:
 *   - Track short-term and long-term EMAs per symbol.
 *   - Generate long signals on bullish EMA crossovers.
 *   - Generate short/exit signals on bearish crossovers.
 *   - Configurable EMA periods, position sizing, and stop-loss.
 *
 * Inputs:  EvaluationContext with symbol bar/quote state.
 * Outputs: StrategySignal or null (not yet implemented).
 */

export {};

// TODO: Implement MomentumStrategy extends BaseStrategy
// TODO: Implement createMomentumConfig()
// TODO: Define MomentumStrategyConfig interface
